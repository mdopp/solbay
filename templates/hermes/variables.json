{
  "HERMES_API_PORT": {
    "type": "text",
    "description": "Host port for Hermes' HTTP API. Default 8642. Bound to 127.0.0.1 (API_SERVER_HOST in template.yml). Other ServiceBay pods on this host reach it via host loopback; remote callers go through NPM + Authelia.",
    "default": "8642"
  },
  "HERMES_API_KEY": {
    "type": "secret",
    "description": "Bearer token clients send as `Authorization: Bearer <key>` on requests to Hermes' API. Auto-generated; the wizard surfaces it as a credential so operators can paste it into Solilos's `solbay` config (or any other client). Regenerate from the wizard if it leaks."
  },
  "CONFIG_AGENT_IMAGE": {
    "type": "text",
    "description": "Image for the Solilos config sidecar that runs in the hermes pod (applies admin soul/model edits from the chat panel — the public chat pod can't write Hermes' data dir). Reuses the solilos-chat image with a different entrypoint. Leave default unless running a fork PoC.",
    "default": "ghcr.io/mdopp/solilos-chat:latest"
  },
  "CONFIG_AGENT_PORT": {
    "type": "text",
    "description": "Host loopback port the config sidecar binds (the chat pod reaches it here via host loopback; both pods are hostNetwork). Default 8650; change only if it clashes.",
    "default": "8650"
  },
  "HERMES_LLM_PROVIDER_URL": {
    "type": "text",
    "description": "OpenAI-compatible LLM endpoint Hermes calls. Default points at the `ollama` template on the same host (127.0.0.1:11434 is reachable because both pods are hostNetwork). Override to point at a remote vLLM, a different Ollama, or a cloud provider — the post-deploy script writes this into Hermes' config.yaml.",
    "default": "http://127.0.0.1:11434/v1"
  },
  "OLLAMA_DEFAULT_MODEL": {
    "type": "text",
    "description": "Model tag Hermes writes into `config.yaml`'s `model.model`. Shares its name with the `ollama` template's variable, so ServiceBay prompts for it once and the tag Hermes requests is exactly the one Ollama pre-pulls — the two can't drift apart. Default `gemma4:12b` (multimodal, fits a 16 GB GPU; `gemma4:e2b` is a lighter text/vision alternative). Only override if you point `HERMES_LLM_PROVIDER_URL` at a non-Ollama backend.",
    "default": "gemma4:12b"
  },
  "HERMES_DASHBOARD_PORT": {
    "type": "text",
    "description": "Host loopback port for Hermes' web dashboard (config, API keys, sessions). Bound to 127.0.0.1 only — reached through NPM + Authelia forward-auth at https://<HERMES_SUBDOMAIN>.<domain>, never directly on the LAN.",
    "default": "9119"
  },
  "HERMES_SUBDOMAIN": {
    "type": "subdomain",
    "description": "Subdomain for the Hermes web dashboard. Internal exposure — it gets a real LE cert (Authelia forward-auth needs https) but NPM binds the LAN-only access list, so it isn't reachable from outside the LAN. Authelia forward-auth gates it on top of that, because the dashboard surfaces Hermes' API keys.",
    "default": "hermes",
    "exposure": "internal",
    "proxyPort": "HERMES_DASHBOARD_PORT",
    "loopbackOnly": true,
    "proxyConfig": {
      "allow_websocket_upgrade": true,
      "block_exploits": true,
      "ssl_forced": true,
      "advanced_config": "__authelia_forward_auth__",
      "strictUpstreamHost": true
    }
  },
  "HASS_URL": {
    "type": "text",
    "description": "Optional: URL of your Home Assistant instance. Defaults to http://127.0.0.1:8123 (reachable because both pods share hostNetwork). When combined with HASS_TOKEN, turns on Hermes' native Home Assistant integration for device control on first start.",
    "default": "http://127.0.0.1:8123"
  },
  "HASS_TOKEN": {
    "type": "secret",
    "noAutoGenerate": true,
    "description": "Optional: Home Assistant long-lived access token. Auto-populated from home-assistant's onboarding flow (see post-deploy adoption logic) — leave blank in the wizard. Manually setting a value here pins it and disables the adoption."
  },
  "JELLYFIN_URL": {
    "type": "text",
    "description": "Optional: URL of your Jellyfin media server. When set together with JELLYFIN_USERNAME, the post-deploy auto-installs Home Assistant's `jellyfin` integration via HA's config-flow API, so Sol can control playback and read now-playing through `media_player.jellyfin_*` entities (no extra Solilos code). Default points at a Jellyfin running on this host (both pods share hostNetwork). Leave blank to skip the integration.",
    "default": "http://127.0.0.1:8096"
  },
  "JELLYFIN_USERNAME": {
    "type": "text",
    "description": "Optional: Jellyfin username the Home Assistant `jellyfin` integration logs in as. Pair with JELLYFIN_URL and JELLYFIN_PASSWORD. Empty skips the integration (post-deploy fails soft).",
    "default": ""
  },
  "JELLYFIN_PASSWORD": {
    "type": "secret",
    "noAutoGenerate": true,
    "description": "Optional: password for JELLYFIN_USERNAME. The HA `jellyfin` config flow treats it as optional (blank is accepted for passwordless Jellyfin users), so leave it empty if that account has no password. Only used to auto-install the HA Jellyfin integration."
  },
  "TELEGRAM_BOT_TOKEN": {
    "type": "secret",
    "noAutoGenerate": true,
    "description": "Optional: Telegram bot token from @BotFather. When set, Hermes' Telegram gateway starts and routes inbound chats to Solilos. Pair with TELEGRAM_ALLOWED_USERS — without an allowlist Hermes denies every message and logs `No user allowlists configured`. Leave blank if you don't use Telegram (post-deploy skips writing the .env entry; gateway stays off)."
  },
  "TELEGRAM_ALLOWED_USERS": {
    "type": "text",
    "description": "Optional: comma-separated Telegram user IDs allowed to chat with Hermes (e.g. `123456789,987654321`). Use @userinfobot to find your numeric ID. Empty value means every Telegram message is denied even if TELEGRAM_BOT_TOKEN is set.",
    "default": ""
  },
  "DISCORD_BOT_TOKEN": {
    "type": "secret",
    "noAutoGenerate": true,
    "description": "Optional: Discord bot token from the Discord developer portal. When set, Hermes' Discord gateway starts. Pair with DISCORD_ALLOWED_CHANNELS to allowlist the channels Hermes responds in. Leave blank if you don't use Discord."
  },
  "DISCORD_ALLOWED_CHANNELS": {
    "type": "text",
    "description": "Optional: comma-separated Discord channel IDs Hermes is allowed to respond in. Empty means every channel is denied even if DISCORD_BOT_TOKEN is set.",
    "default": ""
  },
  "SIGNAL_ACCOUNT": {
    "type": "text",
    "description": "Optional: Signal phone number (E.164, e.g. `+491701234567`) the linked signal-cli account belongs to. Linking itself is interactive (`podman exec -it hermes signal-cli link -n HermesAgent` + scan QR with the operator's phone — see README). Setting this here writes the Hermes-side config; pair with SIGNAL_ALLOWED_USERS.",
    "default": ""
  },
  "SIGNAL_ALLOWED_USERS": {
    "type": "text",
    "description": "Optional: comma-separated Signal phone numbers (E.164) allowed to chat with Hermes. Empty means every Signal message is denied even after pairing.",
    "default": ""
  }
}
